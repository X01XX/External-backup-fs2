Added structs for mask and state.  The structs contain the number of bits they use.

A pn=1 square should be considered changed at the second sample, since it will be incompatible to a pn=2 square.
 
